"""Package marker for the api-gateway application code.

This file allows imports like `from src.routes import ...` to work when the
package is executed as a module (python -m src.app).
"""

# intentionally empty
